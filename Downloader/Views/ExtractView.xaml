<UserControl x:Class="Downloader.Views.ExtractView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignWidth="636" d:DesignHeight="264">
    <StackPanel Margin="12">
		<TextBlock>Extracting files...</TextBlock>
		<ProgressBar x:Name="TotalExtracted" Minimum="0" Maximum="{Binding TotalFiles}" Height="24" Margin="0 12 0 0"/>
		<TextBlock x:Name="CurrentFileName" Margin="0 12 0 0"/>
	</StackPanel>
</UserControl>
